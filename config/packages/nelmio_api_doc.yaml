nelmio_api_doc:
    documentation:
        host: localhost:8000
        schemes: [http]
        info:
            title: bileMo API
            description: This is an awesome app!
            version: 1.0.0
        securityDefinitions:
            Bearer:
                type: apiKey
                description: 'Value: Bearer {jwt}'
                name: Authorization
                in: header
        security:
            - Bearer: []
    # areas:
    #     path_patterns: # an array of regexps
    #         - ^/api(?!/doc$)
    #     host_patterns:
    #         - ^api\.    